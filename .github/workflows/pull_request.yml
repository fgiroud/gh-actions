name: Test CI

on: [push]
env:
  CYPRESS_PROJECT_ID: ${{ secrets.CYPRESS_PROJECT_ID }}
jobs:
  job1:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: if [ -z "$CYPRESS_PROJECT_ID" ]; then echo "::set-output name=matrix::{\"browser\":[\"chrome\", \"edge\"], \"machines\":[1]}"; else echo "::set-output name=matrix::{\"browser\":[\"chrome\", \"edge\"], , \"machines\":[1,2,3]}"; fi
  job2:
    needs: job1
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{fromJSON(needs.job1.outputs.matrix)}}
    steps:
      - name: ${{ matrix.browser }}-${{ matrix.machine }}
        run: echo I am running on machine "${{ matrix.browser }}"

  # prepare_ui_tests_matrix:
  #   runs-on: ubuntu-latest
  #   outputs:
  #     matrix: ${{ steps.set-matrix.outputs.matrix }}
  #   steps:
  #     - id: set-matrix
  #       run: echo "::set-output name=matrix::[1,2,3,4]"

  # ui_tests:
  #   strategy:
  #     # fail-fast: false
  #     matrix:
  #       browser: ['chrome', 'edge']
  #       machines: ${{fromJSON(needs.prepare_ui_tests_matrix.outputs.matrix)}}
  #   runs-on: ubuntu-latest
